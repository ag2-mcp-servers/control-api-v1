# generated by fastapi-codegen:
#   filename:  openapi.yaml
#   timestamp: 2025-06-28T08:57:33+00:00

from __future__ import annotations

from enum import Enum
from typing import Any, Dict, List, Literal, Optional, Union

from pydantic import BaseModel, ConfigDict, Field, RootModel


class RequestMode(Enum):
    single = 'single'


class RuleType(Enum):
    amqp_external = 'amqp/external'


class Status(Enum):
    enabled = 'enabled'
    disabled = 'disabled'


class Header(BaseModel):
    name: Optional[str] = Field(None, description='The name of the header.')
    value: Optional[str] = Field(None, description='The value of the header.')


class Target(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    mandatoryRoute: Optional[bool] = Field(
        None,
        description='Reject delivery of the message if the route does not exist, otherwise fail silently.',
    )
    messageTtl: Optional[int] = Field(
        None,
        description='You can optionally override the default TTL on a queue and specify a TTL in minutes for messages to be persisted. It is unusual to change the default TTL, so if this field is left empty, the default TTL for the queue will be used.',
    )
    persistentMessages: Optional[bool] = Field(
        None,
        description='Marks the message as persistent, instructing the broker to write it to disk if it is in a durable queue.',
    )
    routingKey: Optional[str] = Field(
        None,
        description='The AMQP routing key. See this <a href="https://knowledge.ably.com/what-is-the-format-of-the-routingkey-for-an-amqp-or-kinesis-reactor-rule">Ably knowledge base article</a> for details.',
    )
    url: Optional[str] = None


class Target1(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    mandatoryRoute: bool = Field(
        ...,
        description='Reject delivery of the message if the route does not exist, otherwise fail silently.',
    )
    messageTtl: Optional[int] = Field(
        None,
        description='You can optionally override the default TTL on a queue and specify a TTL in minutes for messages to be persisted. It is unusual to change the default TTL, so if this field is left empty, the default TTL for the queue will be used.',
    )
    persistentMessages: bool = Field(
        ...,
        description='Marks the message as persistent, instructing the broker to write it to disk if it is in a durable queue.',
    )
    routingKey: str = Field(
        ...,
        description='The AMQP routing key. See this <a href="https://knowledge.ably.com/what-is-the-format-of-the-routingkey-for-an-amqp-or-kinesis-reactor-rule">Ably knowledge base article</a> for details.',
    )
    url: str


class Target2(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    mandatoryRoute: bool = Field(
        ...,
        description='Reject delivery of the message if the route does not exist, otherwise fail silently.',
    )
    messageTtl: Optional[int] = Field(
        None,
        description='You can optionally override the default TTL on a queue and specify a TTL in minutes for messages to be persisted. It is unusual to change the default TTL, so if this field is left empty, the default TTL for the queue will be used.',
    )
    persistentMessages: bool = Field(
        ...,
        description='Marks the message as persistent, instructing the broker to write it to disk if it is in a durable queue.',
    )
    routingKey: str = Field(
        ...,
        description='The AMQP routing key. See this <a href="https://knowledge.ably.com/what-is-the-format-of-the-routingkey-for-an-amqp-or-kinesis-reactor-rule">Ably knowledge base article</a> for details.',
    )
    url: str


class RuleType3(Enum):
    amqp = 'amqp'


class Target3(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    queueId: Optional[str] = None


class Target4(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    queueId: str


class Target5(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    queueId: str


class AppPatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    apnsCertificate: Optional[str] = Field(
        None, description='The Apple Push Notification service certificate.'
    )
    apnsPrivateKey: Optional[str] = Field(
        None, description='The Apple Push Notification service private key.'
    )
    apnsUseSandboxEndpoint: Optional[bool] = Field(
        None, description='The Apple Push Notification service sandbox endpoint.'
    )
    fcmKey: Optional[str] = Field(
        None, description='The Firebase Cloud Messaging key.', examples=[False]
    )
    name: Optional[str] = Field(
        None,
        description='The name of the application for your reference only.',
        examples=['My App'],
    )
    status: Optional[Status] = Field(
        None,
        description='The status of the application. Can be `enabled` or `disabled`. Enabled means available to accept inbound connections and all services are available.',
        examples=['enabled'],
    )
    tlsOnly: Optional[bool] = Field(
        None, description='Enforce TLS for all connections.', examples=[True]
    )


class AppPkcs12(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    p12File: bytes = Field(
        ..., description="The `.p12` file containing the app's APNs information."
    )
    p12Pass: str = Field(
        ..., description='The password for the corresponding `.p12` file.'
    )


class AppPost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    apnsCertificate: Optional[str] = Field(
        None, description='The Apple Push Notification service certificate.'
    )
    apnsPrivateKey: Optional[str] = Field(
        None, description='The Apple Push Notification service private key.'
    )
    apnsUseSandboxEndpoint: Optional[bool] = Field(
        None, description='The Apple Push Notification service sandbox endpoint.'
    )
    fcmKey: Optional[str] = Field(
        None, description='The Firebase Cloud Messaging key.', examples=[False]
    )
    name: str = Field(
        ...,
        description='The name of the application for your reference only.',
        examples=['My App'],
    )
    status: Optional[Status] = Field(
        None,
        description='The status of the application. Can be `enabled` or `disabled`. Enabled means available to accept inbound connections and all services are available.',
        examples=['enabled'],
    )
    tlsOnly: Optional[bool] = Field(
        None, description='Enforce TLS for all connections.', examples=[True]
    )


class AppResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(
        None,
        alias='_links',
        description='A link self-referencing the app that has been created.',
    )
    accountId: Optional[str] = Field(
        None, description='The ID of your Ably account.', examples=['WgRpOB']
    )
    apnsUseSandboxEndpoint: Optional[bool] = Field(
        None, description='Apple Push Notification service endpoint.', examples=[False]
    )
    id: Optional[str] = Field(
        None, description='The application ID.', examples=['28AB6x']
    )
    name: Optional[str] = Field(
        None, description='The application name.', examples=['Default']
    )
    status: Optional[Status] = Field(
        None,
        description='The application status. Disabled applications will not accept new connections and will return an error to all clients.',
        examples=['enabled'],
    )
    tlsOnly: Optional[bool] = Field(
        None,
        description='Enforce TLS for all connections. This setting overrides any channel setting.',
        examples=[True],
    )


class AuthenticationMode(Enum):
    credentials = 'credentials'


class AwsAccessKeys(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    accessKeyId: str = Field(
        ...,
        description='The AWS key ID for the AWS IAM user. See this <a href="https://knowledge.ably.com/authentication-for-reactor-rules-for-aws-reactor-events-for-lambda-functions-reactor-firehose-for-aws-sqs-and-kinesis">Ably knowledge base article</a> for details.',
    )
    authenticationMode: Literal['credentials'] = Field(
        ...,
        description='Authentication method is using AWS credentials (AWS key ID and secret key).',
    )
    secretAccessKey: str = Field(
        ...,
        description='The AWS secret key for the AWS IAM user. See this <a href="https://knowledge.ably.com/authentication-for-reactor-rules-for-aws-reactor-events-for-lambda-functions-reactor-firehose-for-aws-sqs-and-kinesis">Ably knowledge base article</a> for details.',
    )


class AwsAccessKeysResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    accessKeyId: Optional[str] = Field(
        None,
        description='The AWS key ID for the AWS IAM user. See this <a href="https://knowledge.ably.com/authentication-for-reactor-rules-for-aws-reactor-events-for-lambda-functions-reactor-firehose-for-aws-sqs-and-kinesis">Ably knowledge base article</a> for details.',
    )
    authenticationMode: Literal['credentials'] = Field(
        ...,
        description='Authentication method is using AWS credentials (AWS key ID and secret key).',
    )


class AuthenticationMode2(Enum):
    assumeRole = 'assumeRole'


class AwsAssumeRole(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    assumeRoleArn: str = Field(
        ...,
        description='If you are using the "ARN of an assumable role" authentication method, this is your Assume Role ARN. See this <a href="https://knowledge.ably.com/authentication-for-reactor-rules-for-aws-reactor-events-for-lambda-functions-reactor-firehose-for-aws-sqs-and-kinesis">Ably knowledge base article</a> for details.',
    )
    authenticationMode: Literal['assumeRole'] = Field(
        ...,
        description='Authentication method is using the ARN of an assumable role. See this <a href="https://knowledge.ably.com/authentication-for-reactor-rules-for-aws-reactor-events-for-lambda-functions-reactor-firehose-for-aws-sqs-and-kinesis">Ably knowledge base article</a> for details.',
    )


class RuleType6(Enum):
    aws_kinesis = 'aws/kinesis'


class Format(Enum):
    json = 'json'


class Target6(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Optional[Union[AwsAccessKeys, AwsAssumeRole]] = Field(
        None, discriminator='authenticationMode'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format] = Field(
        None, description='JSON provides a text-based encoding.'
    )
    partitionKey: Optional[str] = Field(
        None,
        description='The AWS Kinesis partition key. See this <a href="https://knowledge.ably.com/what-is-the-format-of-the-routingkey-for-an-amqp-or-kinesis-reactor-rule">Ably knowledge base article</a> for details.',
    )
    region: Optional[str] = Field(
        None,
        description='The region is which AWS Kinesis is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )
    streamName: Optional[str] = Field(
        None, description='The name of your AWS Kinesis Stream.'
    )


class Target7(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Union[AwsAccessKeys, AwsAssumeRole] = Field(
        ..., discriminator='authenticationMode'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Format = Field(..., description='JSON provides a text-based encoding.')
    partitionKey: str = Field(
        ...,
        description='The AWS Kinesis partition key. See this <a href="https://knowledge.ably.com/what-is-the-format-of-the-routingkey-for-an-amqp-or-kinesis-reactor-rule">Ably knowledge base article</a> for details.',
    )
    region: str = Field(
        ...,
        description='The region is which AWS Kinesis is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )
    streamName: str = Field(..., description='The name of your AWS Kinesis Stream.')


class Target8(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Union[AwsAccessKeysResponse, AwsAssumeRole] = Field(
        ..., discriminator='authenticationMode'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Format = Field(..., description='JSON provides a text-based encoding.')
    partitionKey: str = Field(
        ...,
        description='The AWS Kinesis partition key. See this <a href="https://knowledge.ably.com/what-is-the-format-of-the-routingkey-for-an-amqp-or-kinesis-reactor-rule">Ably knowledge base article</a> for details.',
    )
    region: str = Field(
        ...,
        description='The region is which AWS Kinesis is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )
    streamName: str = Field(..., description='The name of your AWS Kinesis Stream.')


class RuleType9(Enum):
    aws_lambda = 'aws/lambda'


class Target9(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Union[AwsAccessKeys, AwsAssumeRole] = Field(
        ..., discriminator='authenticationMode'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    functionName: str = Field(..., description='The name of your AWS Lambda Function.')
    region: str = Field(
        ...,
        description='The region is which your AWS Lambda Function is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )


class Target11(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Union[AwsAccessKeysResponse, AwsAssumeRole] = Field(
        ..., discriminator='authenticationMode'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    functionName: str = Field(..., description='The name of your AWS Lambda Function.')
    region: str = Field(
        ...,
        description='The region is which your AWS Lambda Function is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )


class RuleType12(Enum):
    aws_sqs = 'aws/sqs'


class Target12(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Optional[Union[AwsAccessKeys, AwsAssumeRole]] = Field(
        None, discriminator='authenticationMode'
    )
    awsAccountId: Optional[str] = Field(None, description='Your AWS account ID.')
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    queueName: Optional[str] = Field(None, description='The AWS SQS queue name.')
    region: Optional[str] = Field(
        None,
        description='The region is which AWS SQS is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )


class Target13(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Union[AwsAccessKeys, AwsAssumeRole] = Field(
        ..., discriminator='authenticationMode'
    )
    awsAccountId: str = Field(..., description='Your AWS account ID.')
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    queueName: str = Field(..., description='The AWS SQS queue name.')
    region: str = Field(
        ...,
        description='The region is which AWS SQS is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )


class Target14(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authentication: Union[AwsAccessKeysResponse, AwsAssumeRole] = Field(
        ..., discriminator='authenticationMode'
    )
    awsAccountId: str = Field(..., description='Your AWS account ID.')
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[str] = None
    queueName: str = Field(..., description='The AWS SQS queue name.')
    region: str = Field(
        ...,
        description='The region is which AWS SQS is hosted. See the <a href="https://docs.aws.amazon.com/general/latest/gr/rande.html#lambda_region">AWS documentation</a> for more detail.',
        examples=['us-west-1'],
    )


class RequestMode15(Enum):
    single = 'single'
    batch = 'batch'


class RuleType15(Enum):
    http_azure_function = 'http/azure-function'


class Target15(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    azureAppId: Optional[str] = Field(
        None,
        description='The Microsoft Azure Application ID. You can find your Microsoft Azure Application ID as shown in this <a href="https://dev.applicationinsights.io/documentation/Authorization/API-key-and-App-ID">article</a>.',
        examples=['d1e9f419-c438-6032b32df979'],
    )
    azureFunctionName: Optional[str] = Field(
        None, description='The name of your Microsoft Azure Function.'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format] = Field(
        None, description='JSON provides a text-based encoding.'
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )


class Target16(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    azureAppId: str = Field(
        ...,
        description='The Microsoft Azure Application ID. You can find your Microsoft Azure Application ID as shown in this <a href="https://dev.applicationinsights.io/documentation/Authorization/API-key-and-App-ID">article</a>.',
        examples=['d1e9f419-c438-6032b32df979'],
    )
    azureFunctionName: str = Field(
        ..., description='The name of your Microsoft Azure Function.'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format] = Field(
        None, description='JSON provides a text-based encoding.'
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )


class Target17(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    azureAppId: str = Field(
        ...,
        description='The Microsoft Azure Application ID. You can find your Microsoft Azure Application ID as shown in this <a href="https://dev.applicationinsights.io/documentation/Authorization/API-key-and-App-ID">article</a>.',
        examples=['d1e9f419-c438-6032b32df979'],
    )
    azureFunctionName: str = Field(
        ..., description='The name of your Microsoft Azure Function.'
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format] = Field(
        None, description='JSON provides a text-based encoding.'
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )


class RuleType18(Enum):
    http_cloudflare_worker = 'http/cloudflare-worker'


class Target18(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: Optional[str] = None


class Target19(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: str


class Target20(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: str


class Error(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    code: int = Field(..., description='The HTTP status code returned.')
    details: Optional[Dict[str, Any]] = Field(
        None, description='Any additional details about the error message.'
    )
    href: str = Field(..., description='The URL to documentation about the error code.')
    message: str = Field(..., description='The error message.')
    statusCode: int = Field(..., description='The Ably error code.')


class RuleType21(Enum):
    http_google_cloud_function = 'http/google-cloud-function'


class Target21(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format] = Field(
        None, description='JSON provides a text-based encoding.'
    )
    functionName: Optional[str] = Field(
        None, description='The name of your Google Cloud Function.'
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    projectId: Optional[str] = Field(
        None,
        description='The project ID for your Google Cloud Project that was generated when you created your project.',
    )
    region: Optional[str] = Field(
        None,
        description='The region in which your Google Cloud Function is hosted. See the <a href="https://cloud.google.com/compute/docs/regions-zones/">Google documentation</a> for more details.',
        examples=['us-west1'],
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )


class Target22(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format] = Field(
        None, description='JSON provides a text-based encoding.'
    )
    functionName: str = Field(
        ..., description='The name of your Google Cloud Function.'
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    projectId: str = Field(
        ...,
        description='The project ID for your Google Cloud Project that was generated when you created your project.',
    )
    region: str = Field(
        ...,
        description='The region in which your Google Cloud Function is hosted. See the <a href="https://cloud.google.com/compute/docs/regions-zones/">Google documentation</a> for more details.',
        examples=['us-west1'],
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )


class Target23(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format] = Field(
        None, description='JSON provides a text-based encoding.'
    )
    functionName: str = Field(
        ..., description='The name of your Google Cloud Function.'
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    projectId: str = Field(
        ...,
        description='The project ID for your Google Cloud Project that was generated when you created your project.',
    )
    region: str = Field(
        ...,
        description='The region in which your Google Cloud Function is hosted. See the <a href="https://cloud.google.com/compute/docs/regions-zones/">Google documentation</a> for more details.',
        examples=['us-west1'],
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )


class RuleType24(Enum):
    http = 'http'


class Format9(Enum):
    json = 'json'
    msgpack = 'msgpack'


class Target24(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Optional[Format9] = Field(
        None,
        description='JSON provides a simpler text-based encoding, whereas MsgPack provides a more efficient binary encoding.',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: Optional[str] = None


class Target25(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Format9 = Field(
        ...,
        description='JSON provides a simpler text-based encoding, whereas MsgPack provides a more efficient binary encoding.',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: str = Field(
        ...,
        description='The URL of the endpoint that is invoked when events occur on Ably.',
    )


class Target26(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    enveloped: Optional[bool] = Field(
        None,
        description='Messages delivered through Reactor are wrapped in an Ably envelope by default that contains metadata about the message and its payload. The form of the envelope depends on whether it is part of a Webhook/Function or a Queue/Firehose rule. For everything besides Webhooks, you can ensure you only get the raw payload by unchecking "Enveloped" when setting up the rule.',
    )
    format: Format9 = Field(
        ...,
        description='JSON provides a simpler text-based encoding, whereas MsgPack provides a more efficient binary encoding.',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: str


class IftttRulePatch(RootModel[Any]):
    root: Any


class RequestMode27(Enum):
    single = 'single'


class RuleType27(Enum):
    http_ifttt = 'http/ifttt'


class Target27(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    eventName: str
    webhookKey: str


class Capability(Enum):
    publish = 'publish'
    subscribe = 'subscribe'
    history = 'history'
    presence = 'presence'
    channel_metadata = 'channel-metadata'
    push_admin = 'push-admin'
    push_subscribe = 'push-subscribe'
    statistics = 'statistics'


class KeyPatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    capabilities: Optional[List[Capability]] = Field(
        None,
        description='The capabilities that this key has. More information on capabilities can be found in the <a href="https://ably.com/documentation/core-features/authentication#capabilities-explained">Ably documentation</a>.',
    )
    channels: Optional[str] = Field(
        None,
        description='Specify the channels and queues that this key can be used with.',
    )
    name: Optional[str] = Field(
        None,
        description='The name for your API key. This is a friendly name for your reference.',
    )


class KeyPost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    capabilities: List[Capability] = Field(
        ...,
        description='The capabilities that this key has. More information on capabilities can be found in the <a href="https://ably.com/documentation/core-features/authentication#capabilities-explained">Ably documentation</a>.',
    )
    channels: str = Field(
        ...,
        description='Specify the channels and queues that this key can be used with.',
    )
    name: str = Field(
        ...,
        description='The name for your API key. This is a friendly name for your reference.',
    )


class CapabilityEnum(Enum):
    publish = 'publish'
    subscribe = 'subscribe'
    history = 'history'
    presence = 'presence'
    channel_metadata = 'channel-metadata'
    push_admin = 'push-admin'
    push_subscribe = 'push-subscribe'
    statistics = 'statistics'


class KeyResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    appId: Optional[str] = Field(
        None,
        description='The Ably application ID which this key is associated with.',
        examples=['28GY6a'],
    )
    capability: Optional[Dict[str, List[CapabilityEnum]]] = Field(
        None,
        description='The capabilities that this key has. More information on capabilities can be found in the <a href="https://ably.com/documentation/core-features/authentication#capabilities-explained">Ably documentation</a>.',
    )
    created: Optional[int] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the key.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The key ID.')
    key: Optional[str] = Field(
        None, description='The complete API key including API secret.'
    )
    modified: Optional[int] = Field(
        None,
        description='Unix timestamp representing the date and time of the last modification of the key.',
        examples=[1614679682091],
    )
    name: Optional[str] = Field(
        None, description='The name of the application this key is associated with.'
    )


class Account(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    id: str = Field(..., description='The account ID.', examples=['VpWaOA'])
    name: str = Field(
        ..., description='The name of the account.', examples=['Free account']
    )


class Token(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    capabilities: List[str] = Field(
        ...,
        description='An array containing the access capabilities associated with the access token.',
        examples=[
            [
                'write:namespace',
                'read:namespace',
                'write:queue',
                'read:queue',
                'write:rule',
                'read:rule',
                'write:key',
                'read:key',
                'write:app',
                'read:app',
            ]
        ],
    )
    id: int = Field(
        ...,
        description='The token ID. This is a UUID.',
        examples=['C95837C9-184B-4CC2-8779-B769F960FADB'],
    )
    name: str = Field(
        ..., description='The friendly name for the token.', examples=['My Token']
    )


class User(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    email: str = Field(
        ..., description='Email address of the user associated with the account.'
    )
    id: int = Field(
        ...,
        description='The user ID associated with the account. This is a UUID.',
        examples=['C95837C9-184B-4CC2-8779-B769F960FADB'],
    )


class Me(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    account: Optional[Account] = None
    token: Optional[Token] = None
    user: Optional[User] = None


class NamespacePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authenticated: Optional[bool] = Field(
        False,
        description='If `true`, clients will not be permitted to use (including to attach, publish, or subscribe) any channels within this namespace unless they are identified, that is, authenticated using a client ID. See the <a href="https://knowledge.ably.com/authenticated-and-identified-clients">Ably knowledge base/a> for more details.',
        examples=[False],
    )
    persistLast: Optional[bool] = Field(
        False,
        description='If `true`, the last message published on a channel will be stored for 365 days. You can access the stored message only by using the channel rewind mechanism and attaching with rewind=1. Please note that for each message stored, an additional message is deducted from your monthly allocation.',
        examples=[False],
    )
    persisted: Optional[bool] = Field(
        False,
        description='If `true`, all messages on a channel will be stored for 24 hours. You can access stored messages via the History API. Please note that for each message stored, an additional message is deducted from your monthly allocation.',
        examples=[False],
    )
    pushEnabled: Optional[bool] = Field(
        False,
        description='If `true`, publishing messages with a push payload in the extras field is permitted and can trigger the delivery of a native push notification to registered devices for the channel.',
        examples=[False],
    )
    tlsOnly: Optional[bool] = Field(
        False,
        description='If `true`, only clients that are connected using TLS will be permitted to subscribe to any channels within this namespace.',
        examples=[False],
    )


class NamespacePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authenticated: Optional[bool] = Field(
        False,
        description='If `true`, clients will not be permitted to use (including to attach, publish, or subscribe) any channels within this namespace unless they are identified, that is, authenticated using a client ID. See the <a href="https://knowledge.ably.com/authenticated-and-identified-clients">Ably Knowledge base</a> for more details.',
        examples=[False],
    )
    id: str = Field(
        ...,
        description='The namespace or channel name that the channel rule will apply to. For example, if you specify `namespace` the namespace will be set to `namespace` and will match with channels `namespace:*` and `namespace`.',
        examples=['namespace'],
    )
    persistLast: Optional[bool] = Field(
        False,
        description='If `true`, the last message published on a channel will be stored for 365 days. You can access the stored message only by using the channel rewind mechanism and attaching with rewind=1. Please note that for each message stored, an additional message is deducted from your monthly allocation.',
        examples=[False],
    )
    persisted: Optional[bool] = Field(
        False,
        description='If `true`, all messages on a channel will be stored for 24 hours. You can access stored messages via the History API. Please note that for each message stored, an additional message is deducted from your monthly allocation.',
        examples=[False],
    )
    pushEnabled: Optional[bool] = Field(
        False,
        description='If `true`, publishing messages with a push payload in the extras field is permitted and can trigger the delivery of a native push notification to registered devices for the channel.',
        examples=[False],
    )
    tlsOnly: Optional[bool] = Field(
        False,
        description='If `true`, only clients that are connected using TLS will be permitted to subscribe to any channels within this namespace.',
        examples=[False],
    )


class NamespaceResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    authenticated: Optional[bool] = Field(
        False,
        description='If `true`, clients will not be permitted to use (including to attach, publish, or subscribe) any channels within this namespace unless they are identified, that is, authenticated using a client ID. See the <a href="https://knowledge.ably.com/authenticated-and-identified-clients">Ably knowledge base</a> for more details.',
        examples=[False],
    )
    created: Optional[int] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the namespace.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(
        None,
        description='The namespace or channel name that the channel rule will apply to. For example, if you specify `namespace` the namespace will be set to `namespace` and will match with channels `namespace:*` and `namespace`.',
        examples=['namespace'],
    )
    modified: Optional[int] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the namespace.',
        examples=[1614679682091],
    )
    persistLast: Optional[bool] = Field(
        False,
        description='If `true`, the last message published on a channel will be stored for 365 days. You can access the stored message only by using the channel rewind mechanism and attaching with rewind=1. Please note that for each message stored, an additional message is deducted from your monthly allocation.',
        examples=[False],
    )
    persisted: Optional[bool] = Field(
        False,
        description='If `true`, all messages on a channel will be stored for 24 hours. You can access stored messages via the History API. Please note that for each message stored, an additional message is deducted from your monthly allocation.',
        examples=[False],
    )
    pushEnabled: Optional[bool] = Field(
        False,
        description='If `true`, publishing messages with a push payload in the extras field is permitted and can trigger the delivery of a native push notification to registered devices for the channel.',
        examples=[False],
    )
    tlsOnly: Optional[bool] = Field(
        False,
        description='If `true`, only clients that are connected using TLS will be permitted to subscribe to any channels within this namespace.',
        examples=[False],
    )


class Queue(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    maxLength: int = Field(
        ..., description='Message limit in number of messages.', examples=[10000]
    )
    name: str = Field(
        ..., description='A friendly name for your queue.', examples=['My queue']
    )
    region: str = Field(
        ...,
        description='The data center region. US East (Virginia) or EU West (Ireland). Values are `us-east-1-a` or `eu-west-1-a`.',
        examples=['us-east-1-a'],
    )
    ttl: int = Field(..., description='TTL in minutes.', examples=[60])


class Amqp(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    queueName: Optional[str] = Field(
        None, description='Name of the Ably queue.', examples=['28AB6w:My queue']
    )
    uri: Optional[str] = Field(
        None,
        description='URI for the AMQP queue interface.',
        examples=['amqps://us-east-1-a-queue.ably.io:5671/shared'],
    )


class Messages(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    ready: Optional[int] = Field(
        None, description='The number of ready messages in the queue.', examples=[0]
    )
    total: Optional[int] = Field(
        None, description='The total number of messages in the queue.', examples=[0]
    )
    unacknowledged: Optional[int] = Field(
        None,
        description='The number of unacknowledged messages in the queue.',
        examples=[0],
    )


class Stats(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    acknowledgementRate: Optional[float] = Field(
        None,
        description='The rate at which messages are acknowledged. Rate is messages per minute.',
    )
    deliveryRate: Optional[float] = Field(
        None,
        description='The rate at which messages are delivered from the queue. Rate is messages per minute.',
    )
    publishRate: Optional[float] = Field(
        None,
        description='The rate at which messages are published to the queue. Rate is messages per minute.',
    )


class Stomp(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    destination: Optional[str] = Field(
        None, description='Destination queue.', examples=['/amqp/queue/28AB6w:My queue']
    )
    host: Optional[str] = Field(
        None, description='The host type for the queue.', examples=['shared']
    )
    uri: Optional[str] = Field(
        None,
        description='URI for the STOMP queue interface.',
        examples=['stomp://us-east-1-a-queue.ably.io:61614'],
    )


class QueueResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    amqp: Optional[Amqp] = None
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28AB6w']
    )
    deadletter: Optional[bool] = Field(
        None,
        description='A boolean that indicates whether this is a dead letter queue or not.',
        examples=[False],
    )
    deadletterId: Optional[str] = Field(
        None, examples=['28AB6w:us-east-1-a:deadletter']
    )
    id: Optional[str] = Field(
        None,
        description='The ID of the Ably queue',
        examples=['28AB6w:us-east-1-a:My queue'],
    )
    maxLength: Optional[int] = Field(
        None, description='Message limit in number of messages.', examples=[10000]
    )
    messages: Optional[Messages] = Field(
        None, description='Details of messages in the queue.'
    )
    name: Optional[str] = Field(
        None, description='The friendly name of the queue.', examples=['My queue']
    )
    region: Optional[str] = Field(
        None,
        description='The data center region for the queue.',
        examples=['eu-west-1-a'],
    )
    state: Optional[str] = Field(
        None, description='The current state of the queue.', examples=['Running']
    )
    stats: Optional[Stats] = None
    stomp: Optional[Stomp] = None
    ttl: Optional[int] = Field(None, description='TTL in minutes.', examples=[60])


class RuleAttributes(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class Type(Enum):
    channel_message = 'channel.message'
    channel_presence = 'channel.presence'
    channel_lifecycle = 'channel.lifecycle'
    channel_occupancy = 'channel.occupancy'


class RuleSource(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    channelFilter: str = Field(
        ...,
        description='This field allows you to filter your rule based on a regular expression that is matched against the complete channel name. Leave this empty if you want the rule to apply to all channels.',
    )
    type: Type = Field(
        ...,
        description='The type `channel.message` delivers all messages published on a channel. The type `channel.presence` delivers all enter, update and leave events for members present on a channel. The type `channel.lifecycle` events for this rule type are currently not supported. Get in touch (https://ably.com/contact) if you need this feature. The type `channel.occupancy` delivers all occupancy events for the channel.',
        examples=['channel.message'],
    )


class RuleSourcePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    channelFilter: Optional[str] = Field(
        None,
        description='This field allows you to filter your rule based on a regular expression that is matched against the complete channel name. Leave this empty if you want the rule to apply to all channels.',
    )
    type: Optional[Type] = Field(
        None,
        description='The type `channel.message` delivers all messages published on a channel. The type `channel.presence` delivers all enter, update and leave events for members present on a channel. The type `channel.lifecycle` events for this rule type are currently not supported. Get in touch (https://ably.com/contact) if you need this feature. The type `channel.occupancy` delivers all occupancy events for the channel.',
    )


class RequestMode29(Enum):
    single = 'single'
    batch = 'batch'


class RuleType29(Enum):
    unsupported = 'unsupported'


class Target29(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    url: str


class UnsupportedRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode29 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['unsupported'] = Field(
        ..., description='This rule type is currently unsupported.'
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target29
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class RuleType30(Enum):
    http_zapier = 'http/zapier'


class Target30(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: Optional[str] = None


class ZapierRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode29] = Field(
        None,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/zapier'] = Field(
        ...,
        description='The type of rule. In this case Zapier. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target30] = None


class Target31(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: str


class ZapierRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode29 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/zapier'] = Field(
        ...,
        description='The type of rule. In this case Zapier. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target31


class Target32(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    headers: Optional[List[Header]] = Field(
        None,
        description="If you have additional information to send, you'll need to include the relevant headers.",
    )
    signingKeyId: Optional[str] = Field(
        None,
        description='The signing key ID for use in `batch` mode. Ably will optionally sign the payload using an API key ensuring your servers can validate the payload using the private API key. See the <a href="https://ably.com/documentation/general/events#security">webhook security docs</a> for more information.',
    )
    url: str


class ZapierRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode29 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/zapier'] = Field(
        ...,
        description='The type of rule. In this case Zapier. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target32
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class AccountsAccountIdAppsGetResponse(RootModel[List[AppResponse]]):
    root: List[AppResponse]


class AppsAppIdKeysGetResponse(RootModel[List[KeyResponse]]):
    root: List[KeyResponse]


class AppsAppIdNamespacesGetResponse(RootModel[List[NamespaceResponse]]):
    root: List[NamespaceResponse]


class AppsAppIdQueuesGetResponse(RootModel[List[QueueResponse]]):
    root: List[QueueResponse]


class AmqpExternalRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode] = Field(
        None,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['amqp/external'] = Field(
        ...,
        description='The type of rule. In this case AMQP external (using Firehose). See the <a href="https://ably.com/documentation/general/firehose">Ably documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target] = None


class AmqpExternalRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['amqp/external'] = Field(
        ...,
        description='The type of rule. In this case AMQP external (using Firehose). See the <a href="https://ably.com/documentation/general/firehose">documentation</a> for further information.',
    )
    source: RuleSource
    target: Target1


class AmqpExternalRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['amqp/external'] = Field(
        ...,
        description='The type of rule. In this case AMQP external (using Firehose). See the <a href="https://ably.com/documentation/general/firehose">Ably documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target2
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class AmqpRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode] = Field(
        None,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['amqp'] = Field(
        ...,
        description='The type of rule. In this case AMQP. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target3] = None


class AmqpRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['amqp'] = Field(
        ...,
        description='The type of rule. In this case AMQP. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target4


class AmqpRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['amqp'] = Field(
        ...,
        description='The type of rule. In this case AMQP. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target5
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class AwsKinesisRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode] = Field(
        None,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/kinesis'] = Field(
        ...,
        description='The type of rule. In this case AWS Kinesis. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target6] = None


class AwsKinesisRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/kinesis'] = Field(
        ...,
        description='The type of rule. In this case AWS Kinesis. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target7


class AwsKinesisRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/kinesis'] = Field(
        ...,
        description='The type of rule. In this case AWS Kinesis. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target8
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class AwsLambdaRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/lambda'] = Field(
        ...,
        description='The type of rule. In this case AWS Lambda. See the <a href="https://ably.com/integrations">Ably documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target9


class AwsLambdaRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/lambda'] = Field(
        ...,
        description='The type of rule. In this case AWS Lambda. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target9


class AwsLambdaRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/lambda'] = Field(
        ...,
        description='The type of rule. In this case AWS Lambda. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target11
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class AwsSqsRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode] = Field(
        None,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/sqs'] = Field(
        ...,
        description='The type of rule. In this case AWS SQS. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target12] = None


class AwsSqsRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/sqs'] = Field(
        ...,
        description='The type of rule. In this case AWS SQS. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target13


class AwsSqsRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['aws/sqs'] = Field(
        ...,
        description='The type of rule. In this case AWS SQS. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target14
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class AzureFunctionRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode15] = Field(
        None,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/azure-function'] = Field(
        ...,
        description='The type of rule. In this case Microsoft Azure Function. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target15] = None


class AzureFunctionRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/azure-function'] = Field(
        ...,
        description='The type of rule. In this case Microsoft Azure Function. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target16


class AzureFunctionRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/azure-function'] = Field(
        ...,
        description='The type of rule. In this case Microsoft Azure Function. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target17
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class CloudflareWorkerRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode15] = Field(
        None,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/cloudflare-worker'] = Field(
        ...,
        description='The type of rule. In this case Cloudflare Worker. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target18] = None


class CloudflareWorkerRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/cloudflare-worker'] = Field(
        ...,
        description='The type of rule. In this case Cloudflare Worker. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target19


class CloudflareWorkerRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/cloudflare-worker'] = Field(
        ...,
        description='The type of rule. In this case Cloudflare Worker. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
        examples=['http/cloudflare-worker'],
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target20
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class GoogleCloudFunctionRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode15] = Field(
        None,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/google-cloud-function'] = Field(
        ...,
        description='The type of rule. In this case Google Cloud Function. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target21] = None


class GoogleCloudFunctionRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/google-cloud-function'] = Field(
        ...,
        description='The type of rule. In this case Google Cloud Function. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    target: Target22


class GoogleCloudFunctionRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/google-cloud-function'] = Field(
        ...,
        description='The type of rule. In this case Google Cloud Function. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target23
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class HttpRulePatch(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: Optional[RequestMode15] = Field(
        None,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http'] = Field(
        ...,
        description='The type of rule. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: Optional[RuleSource] = None
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Optional[Target24] = None


class HttpRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http'] = Field(
        ...,
        description='The type of rule. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target25


class HttpRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode15 = Field(
        ...,
        description='This is Single Request mode or Batch Request mode. Single Request mode sends each event separately to the endpoint specified by the rule. Batch Request mode rolls up multiple events into the same request. You can read more about the difference between single and batched events in the Ably <a href="https://ably.com/documentation/general/events#batching">documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http'] = Field(
        ...,
        description='The type of rule. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target26
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class IftttRulePost(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    requestMode: RequestMode27 = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/ifttt'] = Field(
        ...,
        description='The type of rule. In this case IFTTT. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target27


class IftttRuleResponse(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    field_links: Optional[Dict[str, Any]] = Field(None, alias='_links')
    appId: Optional[str] = Field(
        None, description='The Ably application ID.', examples=['28GY6a']
    )
    created: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of creation of the rule.',
        examples=[1602844091815],
    )
    id: Optional[str] = Field(None, description='The rule ID.', examples=['83IzAB'])
    modified: Optional[float] = Field(
        None,
        description='Unix timestamp representing the date and time of last modification of the rule.',
        examples=[1614679682091],
    )
    requestMode: RequestMode27 = Field(
        ...,
        description='Single request mode sends each event separately to the endpoint specified by the rule. You can read more about single request mode events in the <a href="https://ably.com/documentation/general/events#batching">Ably documentation</a>.',
        examples=['single'],
    )
    ruleType: Literal['http/ifttt'] = Field(
        ...,
        description='The type of rule. In this case IFTTT. See the <a href="https://ably.com/integrations">documentation</a> for further information.',
    )
    source: RuleSource
    status: Optional[Status] = Field(
        None,
        description='The status of the rule. Rules can be enabled or disabled.',
        examples=['enabled'],
    )
    target: Target27
    version: Optional[str] = Field(
        None,
        description='API version. Events and the format of their payloads are versioned. Please see the <a href="https://ably.com/documentation/general/events">Events documentation</a>.',
    )


class RulePatch(
    RootModel[
        Union[
            HttpRulePatch,
            IftttRulePatch,
            ZapierRulePatch,
            CloudflareWorkerRulePatch,
            AzureFunctionRulePatch,
            GoogleCloudFunctionRulePatch,
            AwsLambdaRulePatch,
            AwsKinesisRulePatch,
            AwsSqsRulePatch,
            AmqpRulePatch,
            AmqpExternalRulePatch,
        ]
    ]
):
    root: Union[
        HttpRulePatch,
        IftttRulePatch,
        ZapierRulePatch,
        CloudflareWorkerRulePatch,
        AzureFunctionRulePatch,
        GoogleCloudFunctionRulePatch,
        AwsLambdaRulePatch,
        AwsKinesisRulePatch,
        AwsSqsRulePatch,
        AmqpRulePatch,
        AmqpExternalRulePatch,
    ] = Field(..., discriminator='ruleType')


class RulePost(
    RootModel[
        Union[
            HttpRulePost,
            IftttRulePost,
            ZapierRulePost,
            CloudflareWorkerRulePost,
            AzureFunctionRulePost,
            GoogleCloudFunctionRulePost,
            AwsLambdaRulePost,
            AwsKinesisRulePost,
            AwsSqsRulePost,
            AmqpRulePost,
            AmqpExternalRulePost,
            UnsupportedRuleResponse,
        ]
    ]
):
    root: Union[
        HttpRulePost,
        IftttRulePost,
        ZapierRulePost,
        CloudflareWorkerRulePost,
        AzureFunctionRulePost,
        GoogleCloudFunctionRulePost,
        AwsLambdaRulePost,
        AwsKinesisRulePost,
        AwsSqsRulePost,
        AmqpRulePost,
        AmqpExternalRulePost,
        UnsupportedRuleResponse,
    ] = Field(..., discriminator='ruleType')


class RuleResponse(
    RootModel[
        Union[
            HttpRuleResponse,
            IftttRuleResponse,
            ZapierRuleResponse,
            CloudflareWorkerRuleResponse,
            AzureFunctionRuleResponse,
            GoogleCloudFunctionRuleResponse,
            AwsLambdaRuleResponse,
            AwsKinesisRuleResponse,
            AwsSqsRuleResponse,
            AmqpRuleResponse,
            AmqpExternalRuleResponse,
        ]
    ]
):
    root: Union[
        HttpRuleResponse,
        IftttRuleResponse,
        ZapierRuleResponse,
        CloudflareWorkerRuleResponse,
        AzureFunctionRuleResponse,
        GoogleCloudFunctionRuleResponse,
        AwsLambdaRuleResponse,
        AwsKinesisRuleResponse,
        AwsSqsRuleResponse,
        AmqpRuleResponse,
        AmqpExternalRuleResponse,
    ] = Field(..., discriminator='ruleType')


class AppsAppIdRulesGetResponse(RootModel[List[RuleResponse]]):
    root: List[RuleResponse]
